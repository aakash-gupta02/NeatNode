# Endpoints

Place route definitions under `src/routes` and handlers under `src/controllers`.

Example structure:

```
src/routes/
src/controllers/
```

Add a new route file (for example `src/routes/posts.route.js`) and the corresponding controller (`src/controllers/posts.controller.js`).
`````mdx
# Endpoints

This section documents how REST endpoints are generated and where to place new routes.

Structure:

# Endpoints

Place route definitions under `src/routes` and handlers under `src/controllers`.

Example structure:

```
src/routes/
src/controllers/
```

Add a new route file (e.g., `src/routes/posts.route.js`) and corresponding controller (`src/controllers/posts.controller.js`).
