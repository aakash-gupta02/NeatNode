# Installation (Getting Started)

Install the CLI globally if desired:

```bash
npm i -g neatnode
```

Or run per-project with npx:

```bash
npx neatnode
```
`````mdx
# Installation

Install globally:

```bash
npm i -g neatnode
````

Or use npx (recommended):

```bash
npx neatnode
```
`````
