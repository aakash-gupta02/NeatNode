# CLI Usage (Getting Started)

Run the CLI (interactive):

```bash
npx neatnode
```

Prompts include project name, template selection, and whether to include example CRUD.
`````mdx
# CLI Usage

After installation, run:

```bash
npx neatnode
````

You’ll be prompted to select:

* Project name
* Template type (Basic / REST / Socket.io)
* Whether to include CRUD

Once confirmed, the CLI scaffolds your app instantly.


`````
